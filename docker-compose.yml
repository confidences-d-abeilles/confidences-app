version: '3'
services:
  preview:
    build: .
    image: cda-app:latest
    restart: always
    ports:
      - 5000:5000
    environment:
      REACT_APP_STRIPE_API_KEY: pk_test_mLWoutIWlytgJmEvWuSL3xSB
    labels:
      - "traefik.frontend.rule=Host:cda.cchampou.me"
      - "traefik.backend.port=5000"
      - "traefik.enable=true"
      - "traefik.docker.network=layko_layko_net"

networks:
  default:
    external:
      name: layko_layko_net
