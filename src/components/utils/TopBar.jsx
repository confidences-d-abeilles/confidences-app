import styled from '@emotion/styled';

const TopBar = styled('div')`
  width: 100%;
  background-color: black;
  padding: 0.5rem;
  text-align: center;
  color: silver;
  font-size: 0.9rem;
`;

export default TopBar;
