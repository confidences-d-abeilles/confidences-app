kind: pipeline
name: default

steps:
  - name: build
    image: docker
    commands:
      - docker build . -t cda-app:latest
    volumes:
      - name: docker
        path: /var/run/docker.sock

volumes:
  - name: docker
    host:
      path: /var/run/docker.sock
